# Makefile generated by ucpp: https://github.com/nikitakit/ucpp
# Run `ucpp configure` to regenerate the makefile
# Do not edit!!!
#
# The file ".wrmakefile" is the template used by the Wind River Workbench to
# generate the makefiles of this project. Add user-specific build targets and
# make rules only(!) in this project's ".wrmakefile" file. These will then be
# automatically dumped into the makefiles.

export WIND_HOME=/home/adam/.ucpp/gccdist/WindRiver
export WIND_BASE=/home/adam/.ucpp/gccdist/WindRiver/vxworks-6.3
export WIND_LIC_PROXY=/home/adam/.ucpp/gccdist/WindRiver/setup/x86-win32/bin/
export LM_LICENSE_FILE=/home/adam/.ucpp/gccdist/WindRiver/license/zwrsLicense.lic
export WINEDEBUG=fixme-all
WIND_HOME := $(subst \,/,$(WIND_HOME))
WIND_BASE := $(subst \,/,$(WIND_BASE))
WIND_USR := $(subst \,/,$(WIND_USR))

all : pre_build main_all post_build

_clean ::
	@echo "make: removing targets and objects of `pwd`"

TRACE=0
TRACEON=$(TRACE:0=@)
TRACE_FLAG=$(TRACEON:1=)

MAKEFILE := Makefile

FLEXIBLE_BUILD := 1

BUILD_SPEC = PPC603gnu
DEBUG_MODE = 1
ifeq ($(DEBUG_MODE),1)
MODE_DIR := Debug
else
MODE_DIR := NonDebug
endif
OBJ_DIR := .
PRJ_ROOT_DIR := /home/adam/FRC2014/main
WS_ROOT_DIR := /home/adam/FRC2014



#Global Build Macros
PROJECT_TYPE = DKM
DEFINES = 
EXPAND_DBG = 0


#BuildSpec specific Build Macros
VX_CPU_FAMILY = ppc
CPU = PPC603
TOOL_FAMILY = gnu
TOOL = gnu
TOOL_PATH = wine /home/adam/.ucpp/gccdist/WindRiver/gnu/3.4.4-vxworks-6.3/x86-win32/bin/
CC_ARCH_SPEC = -mcpu=603 -mstrict-align -mno-implicit-fp  -mlongcall
LIBPATH = 
LIBS = 

IDE_INCLUDES = -I$(WIND_BASE)/target/h -I$(WIND_BASE)/target/h/WPILib -I$(WIND_BASE)/target/h/wrn/coreip -I. 

IDE_LIBRARIES = $(WIND_BASE)/target/lib/WPILib.a 



#BuildTool flags
ifeq ($(DEBUG_MODE),1)
DEBUGFLAGS_C-Compiler = -g
DEBUGFLAGS_C++-Compiler = -g
DEBUGFLAGS_Linker = -g
DEBUGFLAGS_Partial-Image-Linker = 
DEBUGFLAGS_Librarian = 
DEBUGFLAGS_Assembler = -g
else
DEBUGFLAGS_C-Compiler = -O2 -fstrength-reduce -fno-builtin
DEBUGFLAGS_C++-Compiler = -O2 -fstrength-reduce -fno-builtin
DEBUGFLAGS_Linker = -O2 -fstrength-reduce -fno-builtin
DEBUGFLAGS_Partial-Image-Linker = 
DEBUGFLAGS_Librarian = 
DEBUGFLAGS_Assembler = -O2 -fstrength-reduce -fno-builtin
endif


#Project Targets
PROJECT_TARGETS = main/$(MODE_DIR)/main.out \
	 main_partialImage/$(MODE_DIR)/main_partialImage.o


#Rules

# main
ifeq ($(DEBUG_MODE),1)
main/$(MODE_DIR)/% : DEBUGFLAGS_C-Compiler = -g
main/$(MODE_DIR)/% : DEBUGFLAGS_C++-Compiler = -g
main/$(MODE_DIR)/% : DEBUGFLAGS_Linker = -g
main/$(MODE_DIR)/% : DEBUGFLAGS_Partial-Image-Linker = 
main/$(MODE_DIR)/% : DEBUGFLAGS_Librarian = 
main/$(MODE_DIR)/% : DEBUGFLAGS_Assembler = -g
else
main/$(MODE_DIR)/% : DEBUGFLAGS_C-Compiler = -O2 -fstrength-reduce -fno-builtin
main/$(MODE_DIR)/% : DEBUGFLAGS_C++-Compiler = -O2 -fstrength-reduce -fno-builtin
main/$(MODE_DIR)/% : DEBUGFLAGS_Linker = -O2 -fstrength-reduce -fno-builtin
main/$(MODE_DIR)/% : DEBUGFLAGS_Partial-Image-Linker = 
main/$(MODE_DIR)/% : DEBUGFLAGS_Librarian = 
main/$(MODE_DIR)/% : DEBUGFLAGS_Assembler = -O2 -fstrength-reduce -fno-builtin
endif
main/$(MODE_DIR)/% : IDE_INCLUDES = -I$(WIND_BASE)/target/h -I$(WIND_BASE)/target/h/WPILib -I$(WIND_BASE)/target/h/wrn/coreip -I. 
main/$(MODE_DIR)/% : IDE_LIBRARIES = $(WIND_BASE)/target/lib/WPILib.a 
main/$(MODE_DIR)/% : PROJECT_TYPE = DKM
main/$(MODE_DIR)/% : DEFINES = 
main/$(MODE_DIR)/% : EXPAND_DBG = 0
main/$(MODE_DIR)/% : VX_CPU_FAMILY = ppc
main/$(MODE_DIR)/% : CPU = PPC603
main/$(MODE_DIR)/% : TOOL_FAMILY = gnu
main/$(MODE_DIR)/% : TOOL = gnu
main/$(MODE_DIR)/% : TOOL_PATH = wine /home/adam/.ucpp/gccdist/WindRiver/gnu/3.4.4-vxworks-6.3/x86-win32/bin/
main/$(MODE_DIR)/% : CC_ARCH_SPEC = -mcpu=603 -mstrict-align -mno-implicit-fp  -mlongcall
main/$(MODE_DIR)/% : LIBPATH = 
main/$(MODE_DIR)/% : LIBS = 
main/$(MODE_DIR)/% : OBJ_DIR := main/$(MODE_DIR)

OBJECTS_main = main_partialImage/$(MODE_DIR)/main_partialImage.o

main/$(MODE_DIR)/main.out : $(OBJECTS_main)
	$(TRACE_FLAG)if [ ! -d "`dirname "$@"`" ]; then mkdir -p "`dirname "$@"`"; fi;echo "building $@";rm -f "$@";$(TOOL_PATH)nmppc.exe $(OBJECTS_main) $(IDE_LIBRARIES) | tclsh $(WIND_BASE)/host/resource/hutils/tcl/munch.tcl -c ppc > $(OBJ_DIR)/ctdt.c; $(TOOL_PATH)ccppc.exe $(DEBUGFLAGS_Linker) $(CC_ARCH_SPEC) -fdollars-in-identifiers -Wall $(ADDED_CFLAGS) $(IDE_INCLUDES) $(ADDED_INCLUDES) -DCPU=$(CPU) -DTOOL_FAMILY=$(TOOL_FAMILY) -DTOOL=$(TOOL) -D_WRS_KERNEL   $(DEFINES)  -o $(OBJ_DIR)/ctdt.o -c $(OBJ_DIR)/ctdt.c; $(TOOL_PATH)ccppc.exe -r -nostdlib -Wl,-X -T $(WIND_BASE)/target/h/tool/gnu/ldscripts/link.OUT -o "$@" $(OBJ_DIR)/ctdt.o $(OBJECTS_main) $(IDE_LIBRARIES) $(LIBPATH) $(LIBS)  $(ADDED_LIBPATH) $(ADDED_LIBS) && if [ "$(EXPAND_DBG)" = "1" ]; then plink "$@";fi

main/$(MODE_DIR)/main_compile_file : $(FILE) ;

_clean :: main/$(MODE_DIR)/main_clean

main/$(MODE_DIR)/main_clean : 
	$(TRACE_FLAG)if [ -d "main" ]; then cd "main"; rm -rf $(MODE_DIR); fi


# main_partialImage
ifeq ($(DEBUG_MODE),1)
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_C-Compiler = -g
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_C++-Compiler = -g
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_Linker = -g
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_Partial-Image-Linker = 
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_Librarian = 
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_Assembler = -g
else
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_C-Compiler = -O2 -fstrength-reduce -fno-builtin
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_C++-Compiler = -O2 -fstrength-reduce -fno-builtin
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_Linker = -O2 -fstrength-reduce -fno-builtin
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_Partial-Image-Linker = 
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_Librarian = 
main_partialImage/$(MODE_DIR)/% : DEBUGFLAGS_Assembler = -O2 -fstrength-reduce -fno-builtin
endif
main_partialImage/$(MODE_DIR)/% : IDE_INCLUDES = -I$(WIND_BASE)/target/h -I$(WIND_BASE)/target/h/WPILib -I$(WIND_BASE)/target/h/wrn/coreip -I. -I$(PRJ_ROOT_DIR)
main_partialImage/$(MODE_DIR)/% : IDE_LIBRARIES = $(WIND_BASE)/target/lib/WPILib.a 
main_partialImage/$(MODE_DIR)/% : PROJECT_TYPE = DKM
main_partialImage/$(MODE_DIR)/% : DEFINES = 
main_partialImage/$(MODE_DIR)/% : EXPAND_DBG = 0
main_partialImage/$(MODE_DIR)/% : VX_CPU_FAMILY = ppc
main_partialImage/$(MODE_DIR)/% : CPU = PPC603
main_partialImage/$(MODE_DIR)/% : TOOL_FAMILY = gnu
main_partialImage/$(MODE_DIR)/% : TOOL = gnu
main_partialImage/$(MODE_DIR)/% : TOOL_PATH = wine /home/adam/.ucpp/gccdist/WindRiver/gnu/3.4.4-vxworks-6.3/x86-win32/bin/
main_partialImage/$(MODE_DIR)/% : CC_ARCH_SPEC = -mcpu=603 -mstrict-align -mno-implicit-fp  -mlongcall
main_partialImage/$(MODE_DIR)/% : LIBPATH = 
main_partialImage/$(MODE_DIR)/% : LIBS = 
main_partialImage/$(MODE_DIR)/% : OBJ_DIR := main_partialImage/$(MODE_DIR)

main_partialImage/$(MODE_DIR)/Objects/main/main.o : /home/adam/FRC2014/main/main.cpp
	$(TRACE_FLAG)if [ ! -d "`dirname "$@"`" ]; then mkdir -p "`dirname "$@"`"; fi;echo "building $@"; $(TOOL_PATH)ccppc.exe $(DEBUGFLAGS_C++-Compiler) $(CC_ARCH_SPEC) -ansi -Wall  -MD -MP -mlongcall $(ADDED_C++FLAGS) $(IDE_INCLUDES) $(ADDED_INCLUDES) -DCPU=$(CPU) -DTOOL_FAMILY=$(TOOL_FAMILY) -DTOOL=$(TOOL) -D_WRS_KERNEL   $(DEFINES) -o "$@" -c "$<"


OBJECTS_main_partialImage = main_partialImage/$(MODE_DIR)/Objects/main/main.o 

main_partialImage/$(MODE_DIR)/main_partialImage.o : $(OBJECTS_main_partialImage)
	$(TRACE_FLAG)if [ ! -d "`dirname "$@"`" ]; then mkdir -p "`dirname "$@"`"; fi;echo "building $@"; $(TOOL_PATH)ccppc.exe -r -nostdlib -Wl,-X  -o "$@" $(OBJECTS_main_partialImage) $(LIBPATH) $(LIBS) $(ADDED_LIBPATH) $(ADDED_LIBS)  && if [ "$(EXPAND_DBG)" = "1" ]; then plink "$@";fi

main_partialImage/$(MODE_DIR)/main_partialImage_compile_file : $(FILE) ;

_clean :: main_partialImage/$(MODE_DIR)/main_partialImage_clean

main_partialImage/$(MODE_DIR)/main_partialImage_clean : 
	$(TRACE_FLAG)if [ -d "main_partialImage" ]; then cd "main_partialImage"; rm -rf $(MODE_DIR); fi

DEP_FILES := main_partialImage/$(MODE_DIR)/Objects/main/main.d 
##DEP_FILES not used on Linux##



-include $(PRJ_ROOT_DIR)/*.makefile

-include *.makefile

main_all : external_build $(PROJECT_TARGETS)
	@echo "make: built targets of `pwd`"

# entry point for extending the build
external_build ::
	@echo ""

# main entry point for pre processing prior to the build
pre_build :: $(PRE_BUILD_STEP) generate_sources
	@echo ""

# entry point for generating sources prior to the build
generate_sources ::
	@echo ""

# main entry point for post processing after the build
post_build :: $(POST_BUILD_STEP) deploy_output
	@echo ""

# entry point for deploying output after the build
deploy_output ::
	@echo ""

clean :: external_clean $(CLEAN_STEP) _clean

# entry point for extending the build clean
external_clean ::
	@echo ""


# Custom targets inserted by ucpp

deploy: $(PROJECT_TARGETS)
	wput -u main/Debug/main.out ftp://anonymous@10.17.86.2/ni-rt/system/FRC_UserProgram.out&&echo reboot|nc -u -q1 10.17.86.2 6668&&echo "Rebooting cRio..."

undeploy:
	ftp 10.17.86.2 < /home/adam/.bin/ucpp/delcode

